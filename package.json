{
  "name": "capbooks",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "@cap-js/hana": "^1",
    "@sap/xssec": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.10.0",
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "npx mbt build",
    "deploy": "cf deploy mta_archives/capbooks_1.0.0.mtar",
    "hana-deploy": "cds deploy --to hana:caphana-dev"
  },
  "sapux": [
    "app/admin-books",
    "app/browse"
  ],
  "cds": {
    "sql": {
      "native_hana_associations": false
    },
    "requires": {
      "auth": "xsuaa"
    }
  }
}
